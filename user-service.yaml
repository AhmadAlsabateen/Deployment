apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service-deployment
  namespace: ahmad
spec:
  replicas: 1
  selector:
    matchLabels:
      bb: user-service
  template:
    metadata:
      labels:
        bb: user-service
    spec:
      containers:
      - name: user-service
        image: ahmad2sabateen/user-service
---
apiVersion: v1
kind: Service
metadata:
  name: user-service-entrypoint
  namespace: ahmad
spec:
  type: LoadBalancer
  selector:
    bb: user-service
  ports:
  - port: 80
    targetPort: 8080