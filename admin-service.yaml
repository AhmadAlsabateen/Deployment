apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      bb: web
  template:
    metadata:
      labels:
        bb: web
    spec:
      containers:
      - name: admin-service
        image: ahmad2sabateen/admin-service
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service-entrypoint
  namespace: default
spec:
  type: LoadBalancer
  selector:
    bb: web
  ports:
  - port: 80
    targetPort: 8080